[Service]
# Load the wall UI from the patched local repo instead of the packaged
# /usr/share/skwd-wall copy (which package updates keep reverting).
Environment=SKWD_SHELL_QML=%h/.config/skwd-wall/shell.qml
